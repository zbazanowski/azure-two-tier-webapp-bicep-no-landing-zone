name: azure-two-tier-webapp

metadata:
  template: bicep

infra:
  provider: bicep
  template: ./infra/main.bicep

services:
  webapp:
    language: js
    path: ./src/app
    host: appservice

environments:
  - name: dev
  - name: prod